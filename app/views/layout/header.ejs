<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Recruit.log Redis</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background: #f8f9fa; }
    .navbar-brand { font-weight: 700; letter-spacing: 0.5px; }
    .card { border: none; box-shadow: 0 1px 4px rgba(0,0,0,.10); }
    .redis-badge { background: #dc382c; color: white; font-size: 0.7em; padding: 2px 6px; border-radius: 3px; vertical-align: middle; }
    .score-muted { font-size: 0.82em; color: #6c757d; }
    code { background: #e9ecef; padding: 1px 5px; border-radius: 3px; }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
      <a class="navbar-brand" href="/">Recruit.log <span class="redis-badge">Redis</span></a>
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/advisors">Advisors</a>
        <a class="nav-link" href="/listings/stats">Listing Stats</a>
      </div>
    </div>
  </nav>
  <div class="container">
